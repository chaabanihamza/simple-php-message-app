<?php
// config.php

// إعدادات الاتصال بقاعدة البيانات
// تأكد من أن هذه القيم تتطابق مع إعدادات قاعدة بياناتك
$servername = "localhost"; // تم التغيير من "db" إلى "localhost" لحل مشكلة الاتصال
$username = "root";      // اسم المستخدم الذي طلبته
$password = "Dotit2025"; // كلمة المرور التي طلبتها
$dbname = "simple_notes_db"; // اسم قاعدة البيانات التي أنشأناها

// إنشاء الاتصال بقاعدة البيانات
$conn = mysqli_connect($servername, $username, $password, $dbname);

// التحقق من الاتصال
if (!$conn) {
    // إذا فشل الاتصال، أوقف تنفيذ السكريبت واعرض رسالة خطأ
    die("خطأ في الاتصال بقاعدة البيانات: " . mysqli_connect_error());
}

// إذا تم الاتصال بنجاح، لا يوجد أي إخراج هنا.
// سيتم استخدام $conn في ملفات PHP الأخرى التي تتطلب هذا الملف.
?>

